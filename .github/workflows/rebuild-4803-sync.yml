name: Rebuild PR 4803 settings

on:
  push:
    branches: [rebuild-4803-seq]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: rebuild-4803-seq
          fetch-depth: 0
      - name: Validate temporary workflow
        run: echo ready
